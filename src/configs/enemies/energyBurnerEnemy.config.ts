import { HSLA } from '../../core/utils/hsla';

export const ENERGYBURNERENEMYCONFIG = {
  color: new HSLA(235, 100, 60, 1),
  energySteals: 12,
  size: 50,

  auraRadius: 200,
  auraColor: new HSLA(260, 50, 50, 0.3),
} as const;

import { HSLA } from '../../core/utils/hsla';

export const ENERGYBURNERENEMYCONFIG = {
  color: new HSLA(220, 100, 50, 1),
  energySteals: 12,
  size: 50,

  auraRadius: 225,
  auraColor: new HSLA(220, 100, 50, 0.3),
} as const;
